# geotips

## Config

- 公開サイトは `js/gas.js` にある既定の GAS URL を使います。
- ローカルで接続先やトークンを変えたい場合は、`.gitignore` 対象の `js/config.js` を使います。
- 設定例は `js/config.example.js` を参照してください。
- 読み込まれるのは `js/config.js` です。`js/confing.js` という名前では読み込まれません。
